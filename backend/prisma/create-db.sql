-- ============================================================
--  UV SCARS — Create Database
--  Run this once in phpMyAdmin or MySQL CLI before db:push
-- ============================================================

CREATE DATABASE IF NOT EXISTS `scars_db`
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
